const { toISOString } = require("core-js/fn/date");

var todo={};
todo.getData=`
oauth2 integration with different services, fetching data from there...
`
todo.auth=`might change, keep track on`
todo.routing=`bcasts - I'm here... neighbors remember. then ask neighbors whether they heard of..
 integer returned is a number of devices to traverse before the target device could be reached.
 perhaps a timestamp of latest activity. `
todo.ml=`
learn, cache, predict and try actions
`
todo.salt=`add salt`
todo.guns=`no propagation, relay, no relay..., one way sync`
